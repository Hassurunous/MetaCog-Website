<!DOCTYPE html>
<html>
    <head>
        <title>MetaCog</title>
        <%= csrf_meta_tags %>

        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <body>
        <nav class="navbar navbar-dark bg-primary navbar-fixed-top">
            <div class="container">
                <%= link_to root_url, class: "navbar-brand col-sm-2" do %>
                <%= image_tag "logo.png", class: "d-inline-block align-top", height: "40" %>
                MetaCog
                <% end %>
                <ul class="nav navbar-nav">
                    <li class="nav-item col-sm-1">
                        <%= link_to "Home", root_url, class: "nav-link" %>
                    </li>
                    <li class="nav-item col-sm-1">
                        <%= link_to "Boards", root_url, class: "nav-link" %>
                    </li>
                    <li class="nav-item col-sm-1">
                        <%= link_to "Resources", root_url, class: "nav-link" %>
                    </li>
                    <li class="nav-item col-sm-1">
                        <%= link_to "About", root_url, class: "nav-link" %>
                    </li>
                </ul>
                <%= form_tag posts_path, :method => 'get', :class => "form-inline float-sm-right", id: "search-form" do %>
                    <%= text_field_tag :search, params[:search], :placeholder => "Search Posts", :class => "form-control" %>
                    <%= submit_tag "Search", :class => "btn btn-secondary" %>
                <% end %>
            </nav>
        </div>
        <div class="container">
            <%= yield %>
        </div>
    </body>
</html>
