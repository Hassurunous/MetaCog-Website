<!DOCTYPE html>
<html>
    <head>
        <title><%= yield(:title) %> | MetaCog</title>
        <%= csrf_meta_tags %>

        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <body>
        <nav class="navbar navbar-dark bg-primary navbar-fixed-top">
            <div class="container">
                <%= link_to root_url, class: "navbar-brand" do %>
                <%= image_tag "logo.png", class: "d-inline-block align-top", height: "40" %>
                MetaCog
                <% end %>
                <ul class="nav navbar-nav">
                    <li class="nav-item">
                        <%= link_to "Home", root_url, class: "nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Posts", posts_url, class: "nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Resources", resources_url, class: "nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "About", about_url, class: "nav-link" %>
                    </li>
                    <li class="nav-item ">
                        <%= link_to "Contact", contact_url, class: "nav-link" %>
                    </li>
                </ul>
                <%= form_tag posts_path, :method => 'get', :class => "form-inline float-sm-right", id: "search-form" do %>
                    <%= text_field_tag :search, params[:search], :placeholder => "Search Posts", :class => "form-control" %>
                    <%= submit_tag "Search", :class => "btn btn-secondary" %>
                <% end %>
            </nav>
        </div>
        <div class="container">
            <%= yield %>
        </div>
    </body>
</html>
